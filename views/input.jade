link(rel="stylesheet", type="text/css", href="/stylesheets/input.css")
script(type="text/javascript", src="/js/input.js")

.container
  .navbar.navbar-fixed-top
    .navbar-inner
      .container
        a.brand(href="/") Vote
        ul.nav
          li
            a(href="/input") 質問を作成する

  br
  br

  .container
    form.well(id="question_form", method="post", action="/confirm")
      h2 質問の内容を入力してください。
      alert.alert-info
        h3 最大で5個まで選択肢を増やせます。
      hr
      fieldset
        label ニックネーム
        input.input-block-level(type="text", id="username", name="username", placeholder="Nick name")
        label パスワード(この質問の編集するときに必要になります)
        input.input-block-level(type="password", id="password", name="password", placeholder="Password")
        label 質問のタイトル
        input.input-block-level(type="text", id="title", name="title", placeholder="Title")
        label 質問の詳細
        textarea.input-block-level(rows="15", id="text", name="text", placeholder="Description")
        label 選択肢(最低2つ必要です)
        input.input-block-level(type="text", id="choice1", name="choice1", placeholder="Choice1")
        input.input-block-level(type="text", id="choice2", name="choice2", placeholder="Choice2")
        input.input-block-level(type="text", id="choice3", name="choice3", placeholder="Choice3")
        input.input-block-level(type="text", id="choice4", name="choice4", placeholder="Choice4")
        input.input-block-level(type="text", id="choice5", name="choice5", placeholder="Choice5")
      hr
      button.btn-large.btn-primary(type="submit") 確認する

  hr

  .footer
    p &copy; Gishhub 2013
